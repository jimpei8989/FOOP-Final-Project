package model.prop;

import model.Pacman;
import utils.Coordinate;

public class SpeedProp extends Prop {
    public SpeedProp(Coordinate coord) {
        super(coord);
    }

    @Override
    public void onPickUp(Pacman p) {
        super.onPickUp(p);
    }
}
